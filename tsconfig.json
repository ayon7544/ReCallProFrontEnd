{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"] // Changed from "src/*" to "./*"
    }
  },
  "include": [
    "**/*", // Changed to include everything
    "nativewind-env.d.ts"
  ]
}
